matrix:
	include:
	- language: node_js
		node_js:
		 - '10'
		before_install:
		- cd oga
		install:
		- yarn install
		script:
		- ./node_modules/.bin/eslint src
		- yarn test --covrage -- watchAll=false
